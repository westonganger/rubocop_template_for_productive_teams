source 'https://rubygems.org'

group :development do
  gem 'rubocop', '1.0.0', require: false # locked to stable version
  gem 'rubocop-minitest', '0.10.1', require: false # locked to stable version
  gem 'rubocop-performance', '1.8.1', require: false # locked to stable version
  gem 'rubocop-rails', '2.8.1', require: false # locked to stable version
  gem 'rubocop-rspec', '2.0.0', require: false # locked to stable version
end
